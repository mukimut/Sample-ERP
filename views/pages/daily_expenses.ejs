<style>
	.card {
		/* Add shadows to create the "card" effect */
		box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
		transition: 0.3s;
	}

	.boxbdymt {
		margin-top: 25px;
	}

	.fa_edt {
		font-size: 22px;
		color: rgb(4, 117, 161);
		font-weight: bold;
	}

	.fa_dlt {
		font-size: 22px;
		color: red;
		font-weight: bold;
	}

	.fa_detail {
		font-size: 24px;
		color: rgb(202, 132, 3);
		font-weight: bold;
	}
</style>
<div class="content-wrapper">
	<section class="content-header">
		<h1>Daily Expenses<small>Settings</small></h1>
		<ol class="breadcrumb">
			<li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
			<li><a href="#">Master Settings</a></li>
		</ol>
	</section>
	<section class="invoice">
		<div class="row">
			<div class="card col-md-6 form-horizontal col-md-offset-3">
				<div class="box-body boxbdymt">
					<div class="form-group">
						<div class="col-sm-12">
							<select id="expense" class="form-control"></select>
						</div>
					</div>
					<div class="form-group">
						<div class="col-sm-12"><input type="text" class="form-control" id="purpose" placeholder="Purpose"></div>
					</div>
					<div class="form-group">
						<div class="col-sm-12">
							<select id="project" class="form-control"></select>
						</div>
					</div>
					<div class="form-group">
						<div class="col-sm-12">
							<select id="customer" class="form-control"></select>
						</div>
					</div>
					<div class="form-group">
						<div class="col-sm-12">
							<select id="supplier" class="form-control"></select>
						</div>
					</div>
					<div class="form-group">
						<div class="col-sm-12">
							<input type="number" class="form-control" id="amount" placeholder="Amount">
						</div>
					</div>
					<div class="row" style="margin-bottom: 20px;">
						<div class="col-md-10">
							<div class="row">
								<div class="box-footer pull-left" style="border-top: 0px; margin-left: 4px;">
									<button class="btn btn-flat btn-success" onclick="addData()">Add</button>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div><br><br><br>

		<div class="row">
			<div class="col-xs-12">
				<div class="box">
					<div class="box-header">
						<h3 class="box-title">Add Daily Expense</h3>
					</div>
					<div class="box-body table-responsive no-padding">
						<table class="table table-hover">
							<thead>
								<th>Expense Type</th>
								<th>Purpose</th>
								<th>Project</th>
								<th>Customer/Supplier</th>
								<th>Amount</th>
								<th>Action</th>
							</thead>
							<tbody id="receiveTable"></tbody>
						</table>
					</div>
				</div>
				<div class="row" style="margin-bottom: 20px;">
					<div class="col-md-10">
						<div class="row">
							<div class="box-footer pull-left" style="border-top: 0px; margin-left: 4px;">
								<button class="btn btn-flat btn-success" onclick="submit()">Save</button>
								<button class="btn btn-flat btn-primary" onclick="edit()" disabled
									id="submitEdit">Edit</button>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>

		<div class="row">
			<div class="col-xs-12">
				<div class="row">
					<div class="box">
						<div class="box-header">
							<h3 class="box-title">List of Daily Expenses</h3>
							<div class="box-tools">
								<div class="input-group input-group-sm" style="width: 150px;">
									<input type="text" name="table_search" class="form-control pull-right"
										placeholder="Search">
									<div class="input-group-btn">
										<button type="submit" class="btn btn-flat btn-success"><i
												class="fa fa-search"></i></button>
									</div>
								</div>
							</div>
						</div>
						<div class="box-body table-responsive no-padding">
							<table class="table table-hover">
								<thead>
									<th>Expense Type</th>
									<th>Purpose</th>
									<th>Project/ Customer</th>
									<th>Amount</th>
									<th>Quantity</th>
									<th>Total</th>
									<th>Action</th>
								</thead>
								<tbody id="receiveTable"></tbody>
							</table>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>
	<div class="clearfix"></div>
</div>

<!-- <div class="control-sidebar-bg"></div> -->
<script src="/dependencies/jquery.js"></script>
<script src="/js/dropdown.js"></script>
<script>
	let addedExpanses = [], allCompanies = [];
	const dropdownIds = ['expense', 'project', 'customer', 'supplier'];
	const inputList = ['purpose', 'amount'];

	setLovData('tov?type=Expense Type', 'expense', 'Select Expense Type');
	setLovData('tov?type=Projects', 'project', 'Select Project');
	
	$.get('/api/companies', (res) => {
		const supplierList = [], customerList = [];
		allCompanies = res.filter(element => {
			const name = element.name, id = element.id;
			let include = true;
			if(element.companytype === 'Customer') customerList.push(name);
			else if(element.companytype === 'Supplier') supplierList.push(name);
			else include = false;
			return include;
		}).map(element => {
			return {id: element.id, name: element.name};
		});

		setSelectionList('supplier', supplierList, 'Select Supplier');
		setSelectionList('customer', customerList, 'Select Customer');
	});

	function logout() {
		localStorage.removeItem('token');
		window.open('/login', '_self');
	}

	function addData() {
		const length = inputList.length;
		let success = true;
		const data = {};

		let warrentyType = 'Warrenty';
		if (document.getElementById('guarantee').checked) warrentyType = 'Guarantee'

		dropdownIds.forEach(id => data[id] = getSelectedText(id));
		const itemEntry = allProducts.find(element => element.name === data.item);
		data['itemid'] = itemEntry.id;
		data['unit'] = itemEntry.unit;
		data['warrenty'] += ' ' + warrentyType;

		for (let i = 0; i < length; i++) {
			const currentValue = document.getElementById(inputList[i]).value;
			if (!currentValue) currentValue = '';
			data[inputList[i]] = currentValue;
		}

		data.unitprice = Number.parseFloat(data.unitprice);
		data.quantity = Number.parseFloat(data.quantity);

		addedItems.push(data);
		updateTable();
	}

	function updateTable() {
		const tableBody = document.getElementById('receiveTable');
		tableBody.innerHTML = '';
		addedItems.forEach((element, index) => {
			let row = tableBody.insertRow();
			let cell;

			cell = row.insertCell();
			cell.append(element.item);
			cell = row.insertCell();
			cell.append(element.brand);
			cell = row.insertCell();
			cell.append(element.unitprice);
			cell = row.insertCell();
			cell.append(element.quantity);
			cell = row.insertCell();
			cell.append(element.unitprice * element.quantity);
			cell = row.insertCell();
			cell.innerHTML = '<button onclick="deleteRow(' + index + ')">Delete</button>'
		});
	}

	function deleteRow(index) {
		addedItems.splice(index, 1);
		updateTable();
	}

	function submit() {
		$.post('/api/receive', {
			data: addedItems.map(element => {
				element.date = moment().format('YYYY-MM-DD');
				return element;
			})
		}, (res) => console.log(res));
	}
</script>